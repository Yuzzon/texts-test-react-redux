import { Container } from '@material-ui/core';
import styled from 'styled-components';

export const SingleTextContainer = styled(Container)`
   margin-top: 30px;
   display: flex;
   flex-direction: column;
   p{
      margin-top: 10px;
      font-size: 110%;
   }
   a{
      margin-bottom: 10px
   }
`
